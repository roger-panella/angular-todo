angular.module("todoListApp", []);   // the instance of the module method creates the application.
                                    // first parameter is name of application
                                    // second parameter (empty array) lets angular know to creates
                                    // an app with the name specified in first parameter

                                    // without the 2nd parameter, angular LOOKS FOR the app rather than
                                    // CREATING it
